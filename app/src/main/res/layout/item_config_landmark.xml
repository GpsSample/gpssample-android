<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
            name="vm"
            type="org.taskforce.episample.config.landmark.LandmarkItemViewModel" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_centerVertical="true"
            android:layout_alignParentStart="true"
            android:id="@+id/landmarkImage"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="8dp"
            android:layout_marginEnd="8dp"
            imageUrl="@{vm.imageUrl}"
            placeholderRes="@{@drawable/icon_landmark_default}"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_toEndOf="@id/landmarkImage"
            android:layout_toStartOf="@id/delete"
            android:textSize="16sp"
            android:text="@{vm.name}"/>

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/delete"
            android:layout_centerVertical="true"
            android:layout_alignParentEnd="true"
            android:layout_marginStart="20dp"
            android:onClick="@{vm::onDeleteClicked}"
            android:src="@drawable/icon_landmark_delete"/>

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/edit"
            android:layout_centerVertical="true"
            android:layout_toStartOf="@id/delete"
            android:onClick="@{vm::onEditClicked}"
            android:src="@drawable/icon_landmark_edit"/>

    </RelativeLayout>

</layout>